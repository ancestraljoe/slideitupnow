# Git Workflow Rules

- Create **separate commits per file** when possible
- Write concise commit messages focusing on "why" not "what"
- Never force-push to main without explicit confirmation
- Always verify JS syntax (`node --check`) before committing
- GitHub Pages deploys automatically on push to main — verify the site works after deploy
